import React, { type ReactElement } from "react";
import { View, Text } from "react-native";

const EmptyListIndicator = (): ReactElement => {
    return (
        <View>
            <Text>Hello World</Text>
        </View>
    );
};

export default EmptyListIndicator;
